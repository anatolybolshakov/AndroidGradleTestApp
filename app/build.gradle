apply plugin: 'java' 

dependencies {
    testCompile 'junit:junit:4.13'
}

test {
    useJUnit()
    filter {
     //include all tests from package
     includeTestsMatching "solutions.s4y.androidempty.*"
    }
}